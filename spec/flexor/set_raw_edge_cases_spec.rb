RSpec.describe Flexor do
  describe "#set_raw" do
    context "with method access on a raw hash" do
      it "method access on a raw Hash value returns the Hash (not a Flexor)"
    end

    context "with interaction with method cache" do
      it "cached getter still returns the raw Hash"
    end
  end
end
